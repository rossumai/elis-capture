import { FlashMode } from './../components/CameraHandler/index';
export const apiUrl = 'https://api.elis.rossum.ai/v1';

// AsyncStorage keys
export const TOKEN: string = 'TOKEN';
export const QUEUE: string = 'QUEUE';
export const FLASHMODE: FlashMode | string = 'FLASHMODE';
